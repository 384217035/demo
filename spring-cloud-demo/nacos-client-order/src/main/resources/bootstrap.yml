spring:
  application:
    name: client-order
  cloud:
    nacos:
      config:
        file-extension: yml
        server-addr: 47.106.150.146:8848
        namespace: nacos-client
        prefix: client-order
        group: client
server:
  servlet:
    context-path: /order
  port: 8082